apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.admin.container.name }}-{{ .Values.environment }}-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.admin.container.name }}
  ports:
    - port: {{ .Values.admin.container.port }}
      protocol: TCP
      targetPort: {{ .Values.admin.container.port }}
